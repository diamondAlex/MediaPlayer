{
    "scripts": {
        "start": "yarn nodemon --ignore ./local/ ./server.js",
        "bg": "pm2 start 'yarn start'",
        "stop": "pm2 stop default"
    },
    "dependencies": {
        "nodemon": "^2.0.22",
        "serve": "^14.2.0"
    },
    "devDependencies": {
        "eslint": "^8.42.0",
        "pm2": "^5.3.0"
    }
}
