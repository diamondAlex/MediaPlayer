{
    "scripts": {
        "start": "yarn nodemon --ignore ./local/ ./server.js",
        "copy": "cp -r ./ /var/www/mediaplayer"
    },
    "dependencies": {},
    "devDependencies": {
        "nodemon": "^2.0.22"
    }
}
